#!/usr/bin/env python

lisT = []

def Pop(one):
	a = one[-1]
	one = one[:-1]
	print 'been separatioon object is %s'  % one
	print 'the last one object is ~$ %s'  % a




lisT = raw_input("input you string:")
if __name__=="__main__":
	Pop(lisT)
